[package]
name = "tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
app = { path = "../app", features = ["ssr"] }
sqlx = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
