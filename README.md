+++
title = "Day 18 - Routing"
description = "Master client-side routing in Leptos to build multi-page single-page applications with navigation, params, and queries."
+++

# Day 18 - Routing

Learn to implement client-side routing in Leptos applications using the Leptos Router. Build SPAs with multiple pages, dynamic routes, nested layouts, and URL parameters.

## Learning Objectives

- Set up Leptos Router for client-side navigation
- Create routes and navigate between pages
- Implement nested routes and parent layouts
- Work with route parameters and dynamic paths
- Handle query parameters and search strings
- Build a complete multi-page application with routing

## Exercises

### Core Exercises
- **01-basics** - Basic routing setup and navigation
- **02-parent-route** - Nested routes with parent layouts
- **03-param** - Dynamic route parameters (e.g., `/users/:id`)
- **04-query** - Query string parameters (e.g., `/search?q=rust`)
- **05-routing-full** - Complete routing application combining all concepts

## Key Concepts

- **Router**: Client-side navigation without page reloads
- **Routes**: URL patterns mapped to components
- **Nested Routes**: Routes within routes for layouts
- **Route Parameters**: Dynamic URL segments (`:id`, `:slug`)
- **Query Parameters**: URL search strings (`?key=value`)
- **Navigation**: Programmatic and declarative routing"# mini-projects" 
