[package]
name = "resend-demo"
version = "0.1.0"
edition = "2021"
description = "Mini demo: send emails with Resend from Rust"

[dependencies]
resend-rs = "0.25"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
clap = { version = "4", features = ["derive"] }
dotenvy = "0.15"
